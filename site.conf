<VirtualHost *:80>
        # The ServerName directive sets the request scheme, hostname and port that
        # the server uses to identify itself. This is used when creating
        # redirection URLs. In the context of virtual hosts, the ServerName
        # specifies what hostname must appear in the request's Host: header to
        # match this virtual host. For the default virtual host (this file) this
        # value is not decisive as it is used as a last resort host regardless.
        # However, you must set it for any further virtual host explicitly.
        # ServerName localhost

        ServerAdmin webmaster@localhost
        DocumentRoot /var/www/html/personalhomepage

        Alias /static "/var/www/html/personalhomepage/frontend/static"


        WSGIScriptAlias / /var/www/html/personalhomepage/personalhomepage/wsgi.py

        # Available loglevels: trace8, ..., trace1, debug, info, notice, warn,
        # error, crit, alert, emerg.
        # It is also possible to configure the loglevel for particular
        # modules, e.g.
        #LogLevel info ssl:warn

        ErrorLog ${APACHE_LOG_DIR}/error.log
        CustomLog ${APACHE_LOG_DIR}/access.log combined

        # For most configuration files from conf-available/, which are
        # enabled or disabled at a global level, it is possible to
        # include a line for only one particular virtual host. For example the
        # following line enables the CGI configuration for this host only
        # after it has been globally disabled with "a2disconf".
        #Include conf-available/serve-cgi-bin.conf
</VirtualHost>

<VirtualHost *:443>
        # The ServerName directive sets the request scheme, hostname and port that
        # the server uses to identify itself. This is used when creating
        # redirection URLs. In the context of virtual hosts, the ServerName
        # specifies what hostname must appear in the request's Host: header to
        # match this virtual host. For the default virtual host (this file) this
        # value is not decisive as it is used as a last resort host regardless.
        # However, you must set it for any further virtual host explicitly.
        # ServerName localhost

        ServerAdmin webmaster@localhost
        DocumentRoot /var/www/html/personalhomepage

        Alias /static "/var/www/html/personalhomepage/frontend/static"


        WSGIScriptAlias / /var/www/html/personalhomepage/personalhomepage/wsgi.py

        # Available loglevels: trace8, ..., trace1, debug, info, notice, warn,
        # error, crit, alert, emerg.
        # It is also possible to configure the loglevel for particular
        # modules, e.g.
        #LogLevel info ssl:warn

        ErrorLog ${APACHE_LOG_DIR}/error.log
        CustomLog ${APACHE_LOG_DIR}/access.log combined

        # For most configuration files from conf-available/, which are
        # enabled or disabled at a global level, it is possible to
        # include a line for only one particular virtual host. For example the
        # following line enables the CGI configuration for this host only
        # after it has been globally disabled with "a2disconf".
        #Include conf-available/serve-cgi-bin.conf

        ServerName ivoplaninic.net

        SSLEngine on

        SSLCertificateFile /var/fullchain.pem
        SSLCertificateKeyFile /var/privkey.pem
</VirtualHost>













<VirtualHost *:80>
        # The ServerName directive sets the request scheme, hostname and port that
        # the server uses to identify itself. This is used when creating
        # redirection URLs. In the context of virtual hosts, the ServerName
        # specifies what hostname must appear in the request's Host: header to
        # match this virtual host. For the default virtual host (this file) this
        # value is not decisive as it is used as a last resort host regardless.
        # However, you must set it for any further virtual host explicitly.
        ServerName ivoplaninic.net

        ServerAdmin admin@ivoplaninic.net
        #DocumentRoot /home/ubuntu/djangoreact-personal-page/personalhomepage/personalhomepage

        Alias /static /home/ubuntu/djangoreact-personal-page/personalhomepage/frontend/static
        Alias /favicon.ico /home/ubuntu/djangoreact-personal-page/personalhomepage/frontend/static/favicon.ico

        <Directory /home/ubuntu/djangoreact-personal-page/personalhomepage/frontend/static>
            Require all granted
        </Directory>


        <Directory /home/ubuntu/djangoreact-personal-page/personalhomepage/personalhomepage>
            <Files wsgi.py>
                Require all granted
            </Files>
        </Directory>

        WSGIDaemonProcess personalhomepage
        WSGIProcessGroup personalhomepage
        WSGIApplicationGroup %{GLOBAL}
        WSGIScriptAlias /a /home/ubuntu/djangoreact-personal-page/personalhomepage/personalhomepage/wsgi.py
        # Available loglevels: trace8, ..., trace1, debug, info, notice, warn,
        # error, crit, alert, emerg.
        # It is also possible to configure the loglevel for particular
        # modules, e.g.
        #LogLevel info ssl:warn

        ErrorLog ${APACHE_LOG_DIR}/error.log
        CustomLog ${APACHE_LOG_DIR}/access.log combined

        # For most configuration files from conf-available/, which are
        # enabled or disabled at a global level, it is possible to
        # include a line for only one particular virtual host. For example the
        # following line enables the CGI configuration for this host only
        # after it has been globally disabled with "a2disconf".
        #Include conf-available/serve-cgi-bin.conf
RewriteEngine on
RewriteCond %{SERVER_NAME} =ivoplaninic.net
RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]
</VirtualHost>

<VirtualHost *:443>
        # The ServerName directive sets the request scheme, hostname and port that
        # the server uses to identify itself. This is used when creating
        # redirection URLs. In the context of virtual hosts, the ServerName
        # specifies what hostname must appear in the request's Host: header to
        # match this virtual host. For the default virtual host (this file) this
        # value is not decisive as it is used as a last resort host regardless.
        # However, you must set it for any further virtual host explicitly.
        #ServerName ivoplaninic.net

        ServerAdmin admin@ivoplaninic.net
        #DocumentRoot /home/ubuntu/schoolcounter

        Alias /static /home/ubuntu/djangoreact-personal-page/personalhomepage/frontend/static
        Alias /favicon.ico /home/ubuntu/djangoreact-personal-page/personalhomepage/frontend/static/favicon.ico
        WSGIScriptAlias / /home/ubuntu/djangoreact-personal-page/personalhomepage/personalhomepage/wsgi.py
        Alias /counter/ /home/ubuntu/schoolcounter/index.html
        <Directory /home/ubuntu/djangoreact-personal-page/personalhomepage/frontend/static>
            Require all granted
        </Directory>

        <Directory /home/ubuntu/schoolcounter>
            <Files index.html>
                Require all granted
            </Files>
        </Directory>


        <Directory /home/ubuntu/djangoreact-personal-page/personalhomepage/personalhomepage>
            <Files wsgi.py>
                Require all granted
            </Files>
        </Directory>

        WSGIProcessGroup personalhomepage
        # Available loglevels: trace8, ..., trace1, debug, info, notice, warn,
        # error, crit, alert, emerg.
        # It is also possible to configure the loglevel for particular
        # modules, e.g.
        #LogLevel info ssl:warn

        ErrorLog ${APACHE_LOG_DIR}/error.log
        CustomLog ${APACHE_LOG_DIR}/access.log combined

        # For most configuration files from conf-available/, which are
        # enabled or disabled at a global level, it is possible to
        # include a line for only one particular virtual host. For example the
        # following line enables the CGI configuration for this host only
        # after it has been globally disabled with "a2disconf".
        #Include conf-available/serve-cgi-bin.conf
ServerName ivoplaninic.net
Include /etc/letsencrypt/options-ssl-apache.conf
SSLCertificateFile /etc/letsencrypt/live/ivoplaninic.net/fullchain.pem
SSLCertificateKeyFile /etc/letsencrypt/live/ivoplaninic.net/privkey.pem
</VirtualHost>